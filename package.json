{
  "name": "mergemaster-js",
  "version": "0.0.2",
  "description": "Merge latest master into open PRs. Resolves package.json and package-lock.json conflicts on GitHub PRs automatically",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ersel Aker",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0",
    "semver": "^5.6.0"
  }
}
